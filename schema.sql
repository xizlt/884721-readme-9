CREATE DATABASE readme;
USE readme;

create table user(
    id INT AUTO_INCREMENT PRIMARY KEY,
    date_registration DATE 
    email
    name
    password
    avatar
    contact
    lot_id
    rate_id
);
